{
  "name": "learn-cli",
  "version": "0.1.0",
  "private": true,
  "description": "Vertical slice: extract atomic knowledge proposals from an Obsidian roadmap via an OpenAI-compatible LLM",
  "type": "module",
  "scripts": {
    "learn": "tsx src/cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "commander": "^12.1.0",
    "markdown-it": "^15.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "happy-dom": "^20.11.6",
    "prettier": "^3.4.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^3.0.5"
  }
}
